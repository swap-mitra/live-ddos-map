{
  "name": "live-ddos-map-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently --kill-others -n \"frontend,backend\" -c \"cyan,green\" \"npm run dev --prefix frontend\" \"cd backend && uvicorn app.main:app --reload\""
  }
}
